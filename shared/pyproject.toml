[project]
name = "ml-contracts"
version = "0.1.0"
description = "Shared schemas for ML services"
requires-python = ">=3.9"
dependencies = ["pydantic>=2.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"